# copy to .env. leave both blank for demo mode.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

GOOGLE_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# optional overrides
# EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
# CHROMA_PERSIST_DIR=/data/chroma

FRONTEND_PORT=8080
